"""Model providers, real and simulated.

:mod:`amg.upstream.simulated` is a simulator and says so in its first line;
every accuracy figure this project publishes is a property of its table.
:mod:`amg.upstream.ollama` is the real, credential-free path used to measure
how far that table sits from a local model answering the same tasks.
"""
