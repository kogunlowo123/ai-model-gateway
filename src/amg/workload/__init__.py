"""The requests, their known-correct answers, and the plans that generate them.

Quality here is exact match against a ground truth the generator computed, not a
rating and not a judge, so correctness is a fact rather than a second estimate
stacked on the first.
"""
